---
name: absorb-writer
description: "DEPRECATED — superseded by user-writer/dev-writer/agent-writer. Stage 4 & 5: Writer instructions for the Absorb pipeline."
---

> **DEPRECATED — not invoked by the current orchestrator.** The live `/absorb` orchestrator invokes track-specific Writer agents (e.g. `user_writer_agent`), never `absorb_writer_agent`. This file is pre-refactor scaffolding, kept for history. See `docs/PIPELINE.md` §4 and §6.

# Stage 4 & 5: Drafting & Expansion

Draft the manual sections based on the outlines. 

**CRITICAL:** Use facts verbatim. Do NOT invent UI elements, buttons, or steps. If a concrete detail is missing from the analysis facts, explicitly insert `[NEEDS VERIFICATION]`. The length of the document is driven by thoroughness and coverage, not a page target.
